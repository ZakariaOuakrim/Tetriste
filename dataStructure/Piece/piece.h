#ifndef PIECE_H
#define PIECE_H

#include <string> // Include the <string> header to use std::string

typedef struct
{
    std::string color; // Use std::string instead of string
    std::string shape; // Use std::string instead of string
} piece;

#endif